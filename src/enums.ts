export enum Role {
  Admin = 'Admin',
  Moderator = 'Moderator',
  User = 'User',
}

export enum Tab {
  ALL = 'all',
  PRIVATE = 'private',
  GROUP = 'group',
}

export enum ChatType {
  PERSONAL = 'personal',
  GROUP = 'group',
}

export enum MsgStatus {
  SENT = 'sent',
  DELIVERED = 'delivered',
  READ = 'read',
  DELETED = 'deleted',
}
